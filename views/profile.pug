extends layout

block content
	.container
		.profile-mainpic
			img.profile-mainpic(src='../images/marblepineapple1.jpg', alt='marble', width='100%', height='100%')
		#IntroText
			h1 Hi, #{user.firstname}!
		.box-profile
		.heading-profile
			h3 Profile
		.profile-picture
			img(src= `${user.profilePicture}` style='width:100%;height:100%')
		.profile-description
			h5 #{user.firstname} #{user.lastname}
			h5 @#{user.username}
			h5 #{user.email}
		.box-recentposts
		.heading-recentposts
			h3 Recent Posts
	//- each blogpost in postList
	//- 	.allposts3
	//- 		.individual-post-title
	//- 			h4 #{blogpost.title}
	//- 		.individual-post-body
	//- 			p #{blogpost.body}
		br
	script.
		$(document).ready(function() {
		$("#IntroText").hide().fadeIn(4000);
		});